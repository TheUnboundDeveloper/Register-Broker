# Documentation images

Screenshots embedded in the top-level `README.md`. All are the first-party
**Reference Console** ([`Test_GUI/ReferenceConsole/`](../../Test_GUI/ReferenceConsole/))
driving the framework as a non-admin client over the broker pipes.

| File | Shows |
|---|---|
| `reference-console-dashboard.png` | Dashboard — live `sensor.readall` (43 sensors) alongside the RGB fleet (DRAM, MSI ARGB header, two Razer devices), driven non-admin over the broker pipes |
