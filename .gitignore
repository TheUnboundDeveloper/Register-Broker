# ---------------------------------------------------------------------------
# Register Broker — repository ignore rules
# ---------------------------------------------------------------------------

# --- Internal engineering log (dev-box paths, session history) ---
CLAUDE.md

# --- Internal planning / thinking artifacts (not shipped) ---
FRAMEWORK-EVOLUTION-PLAN.md

# --- Wiki content (lives in the separate GitHub .wiki.git repo, not here) ---
/wiki/

# The Reference Console (Test_GUI/) and the standalone RgbAudioReactive consumer ARE
# committed — they're first-party demonstrators so anyone can run the GUI, the effects,
# and the audio-reactive tool. Their build output stays ignored by the bin/ + obj/ rules below.

# --- .NET (BrokerSensorBridge) ---
bin/
obj/
publish/
*.user
*.suo
.vs/

# --- WDK / kernel driver (BrokerSmbusDriver) build output ---
x64/
*.sys
*.pdb
*.obj
sdv/
*.sdf
*.VC.db

# --- Signing material: certificates and private keys are NEVER committed ---
*.cer
*.pfx
*.p12

# --- Machine-local configuration / scratch ---
calibration.user.json
out.txt
*.log
SHA256SUMS.local.txt

# --- OS noise ---
Thumbs.db
desktop.ini
.DS_Store
